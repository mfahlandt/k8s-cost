# Go
/bin/
*.out

# Node / frontend
web/node_modules/
web/dist/

# Generated report artifacts (dashboard.json is committed on purpose)
/reports/*.xlsx

# Ingest archive: raw provider exports moved here after ingestion. Kept out of
# git so the large raw CSVs don't bloat the repo — the normalized data lives in
# data/spend/*.json instead.
/data/archive/

# Editor
.idea/
.vscode/
.DS_Store


# GCP service-account keys (never commit)
gcp-key.json
*-key.json
*.sa.json

# Local secrets (tokens/keys) — never commit
.env
